<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hwadee.mapper.SimulationEquipmentMapper">
    <!--namespace根据自己需要创建的的mapper的路径和名称填写-->
    <select id="listSimulationEquipment" resultType="SimulationEquipment">
        select
                simulation_equipment_id,
                name,
               number,
               type,
               software_system,
               version_number,
               supplier,
               status,
               purpose,
               laboratory_id,
               create_time,
               update_time
        from simulation_equipment
    </select>

</mapper>